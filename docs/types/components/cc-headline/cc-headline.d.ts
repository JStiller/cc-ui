import { ComponentInterface } from '../../stencil-public-runtime';
export declare class CcHeadline implements ComponentInterface {
  /**
   * Which rank do you want?
   */
  rank: number;
  private getHeadline;
  render(): HTMLCcHeadlineElement;
}
